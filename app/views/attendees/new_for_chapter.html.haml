%h1= "Attend " + @chapter.name + " event on " + @event.date.to_s + " with:"
.error1= notice
%ul.nav.nav-tabs
  %li= link_to 'Back', events_for_chapter_event_path(@chapter.id), class: "btn"

= render 'form'
